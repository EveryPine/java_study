package first;

public class IfExam {

	public static void main(String[] args) {
		int x = 50;
		int y = 60;
		
		if(x > y) { 
			/* if(조건식){명령} -> 조건식이 true이면 괄호 안 명령을 실행한다. 
			 * 괄호가 없을경우 if 조건식 한칸 밑 명령만 실행한다. */
			System.out.println("x는 y보다 작습니다.");
		}
	}
}
